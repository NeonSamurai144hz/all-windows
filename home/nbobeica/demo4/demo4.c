  #include <stdlib.h>
  #include <stdio.h>

int main() {
	printf("Entrer un mot: \n")
	char mot [100];
	int longeur = 0

	fgets(word, sizeof(word), stdin);

	while(word[length] != '\0') {
	  if(word[length] == '\n') {
	    break;

		}
	length++;
	}

	printf("La longeur du mot est %d\n", longeur )

